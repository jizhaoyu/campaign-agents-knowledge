spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/agentdb
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
app:
  storage:
    base-dir: storage
